import varia
import mathamatics
import termouput
import sys
if len(sys.argv) == 2
    with open(sys.argv[1], "r", encoding="utf-8") as file:
        for line in file:
        # .strip() removes the trailing newline character (\n)
            print(line.strip())
